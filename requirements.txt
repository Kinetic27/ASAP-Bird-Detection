ultralytics>=8.3.0
# Keep the default PyPI resolver away from CUDA 13 wheels on CUDA 12.x systems.
# For a different CUDA stack, install the matching PyTorch build first and then
# install this file with --no-deps or adjust these bounds deliberately.
torch>=2.0.0,<2.7.0
torchvision>=0.15.0,<0.22.0
opencv-python>=4.8.0
PyYAML>=6.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm
pycocotools; platform_system != "Windows"
